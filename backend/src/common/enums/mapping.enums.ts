export enum SyncDirection {
  WIX_TO_HUBSPOT = 'wix_to_hubspot',
  HUBSPOT_TO_WIX = 'hubspot_to_wix',
  BIDIRECTIONAL = 'bidirectional',
}

export enum TransformType {
  NONE = 'none',
  TRIM = 'trim',
  LOWERCASE = 'lowercase',
}
